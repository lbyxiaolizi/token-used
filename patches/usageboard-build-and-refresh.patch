diff --git a/Package.swift b/Package.swift
index 36fecf5..e4d6e73 100644
--- a/Package.swift
+++ b/Package.swift
@@ -1,4 +1,4 @@
-// swift-tools-version: 6.3
+// swift-tools-version: 6.2
 // The swift-tools-version declares the minimum version of Swift required to build this package.
 
 import PackageDescription
diff --git a/Sources/UsageBoardApp/DashboardView.swift b/Sources/UsageBoardApp/DashboardView.swift
index 298556f..955c554 100644
--- a/Sources/UsageBoardApp/DashboardView.swift
+++ b/Sources/UsageBoardApp/DashboardView.swift
@@ -30,6 +30,16 @@ struct DashboardView: View {
         store.configuration.plugins.filter(\.enabled)
     }
 
+    /// 已加载完成且 items 为空的 plugin 自动隐藏（数据为 0 时不占位）。
+    /// idle / loading / failed 状态保留显示，避免首次加载误判。
+    private var visiblePlugins: [PluginConfiguration] {
+        enabledPlugins.filter { plugin in
+            let snap = store.snapshot(for: plugin)
+            if snap.state == .ready, snap.items.isEmpty { return false }
+            return true
+        }
+    }
+
     private var strings: AppLocalization {
         AppLocalization(language: store.activeLanguage)
     }
@@ -43,7 +53,7 @@ struct DashboardView: View {
                 case .grouped:
                     MeasuredScrollView(maxHeight: maxHeight) {
                         LazyVStack(spacing: 10) {
-                            ForEach(enabledPlugins) { plugin in
+                            ForEach(visiblePlugins) { plugin in
                                 PluginGroupView(snapshot: store.snapshot(for: plugin), language: store.activeLanguage) {
                                     store.refresh(pluginID: plugin.id, force: true)
                                 }
@@ -56,7 +66,7 @@ struct DashboardView: View {
                         ScrollViewReader { proxy in
                             ScrollView(.horizontal, showsIndicators: false) {
                                 HStack(spacing: 6) {
-                                    ForEach(enabledPlugins) { plugin in
+                                    ForEach(visiblePlugins) { plugin in
                                         Button {
                                             store.selectedTabID = plugin.id
                                         } label: {
@@ -83,7 +93,7 @@ struct DashboardView: View {
                             .onChange(of: store.selectedTabID) { _ in
                                 scrollToSelectedTab(with: proxy)
                             }
-                            .onChange(of: enabledPlugins.map(\.id)) { _ in
+                            .onChange(of: visiblePlugins.map(\.id)) { _ in
                                 scrollToSelectedTab(with: proxy)
                             }
                         }
@@ -100,8 +110,9 @@ struct DashboardView: View {
         }
         .onAppear {
             ensureSelectedTab()
+            store.refreshAll()
         }
-        .onChange(of: enabledPlugins.map(\.id)) { _ in
+        .onChange(of: visiblePlugins.map(\.id)) { _ in
             ensureSelectedTab()
         }
         .toolbar {
@@ -116,22 +127,22 @@ struct DashboardView: View {
 
     private var selectedPlugin: PluginConfiguration? {
         if let selectedTabID = store.selectedTabID,
-           let plugin = enabledPlugins.first(where: { $0.id == selectedTabID }) {
+           let plugin = visiblePlugins.first(where: { $0.id == selectedTabID }) {
             return plugin
         }
-        return enabledPlugins.first
+        return visiblePlugins.first
     }
 
     private func ensureSelectedTab() {
-        guard !enabledPlugins.isEmpty else {
+        guard !visiblePlugins.isEmpty else {
             store.selectedTabID = nil
             return
         }
         if let selectedTabID = store.selectedTabID,
-           enabledPlugins.contains(where: { $0.id == selectedTabID }) {
+           visiblePlugins.contains(where: { $0.id == selectedTabID }) {
             return
         }
-        store.selectedTabID = enabledPlugins.first?.id
+        store.selectedTabID = visiblePlugins.first?.id
     }
 
     private func scrollToSelectedTab(with proxy: ScrollViewProxy) {
@@ -239,6 +250,42 @@ struct PluginGroupView: View {
         AppLocalization(language: language)
     }
 
+    /// 用 plugin id 作为 AppStorage key 的命名空间，跨刷新持久化用户选择的 period。
+    @AppStorage private var selectedDimensionKey: String
+    init(snapshot: PluginSnapshot, language: AppLanguage, onRefresh: (() -> Void)? = nil) {
+        self.snapshot = snapshot
+        self.language = language
+        self.onRefresh = onRefresh
+        let initial = snapshot.defaultDimension ?? ""
+        self._selectedDimensionKey = AppStorage(
+            wrappedValue: initial,
+            "usageboard.period.\(snapshot.id.uuidString)"
+        )
+    }
+
+    /// 当前生效的 dimension：dimensions 非空时按 selectedDimensionKey；否则 fallback 到 snapshot 顶层。
+    private var activeDimensionKey: String? {
+        guard let dims = snapshot.dimensions, !dims.isEmpty else { return nil }
+        if dims[selectedDimensionKey] != nil { return selectedDimensionKey }
+        if let def = snapshot.defaultDimension, dims[def] != nil { return def }
+        return snapshot.dimensionOrder?.first { dims[$0] != nil } ?? dims.keys.sorted().first
+    }
+    private var activeItems: [UsageItem] {
+        if let key = activeDimensionKey, let dim = snapshot.dimensions?[key] { return dim.items }
+        return snapshot.items
+    }
+    private var activeChart: PluginChart? {
+        if let key = activeDimensionKey, let dim = snapshot.dimensions?[key] { return dim.chart }
+        return snapshot.chart
+    }
+    private var orderedDimensionKeys: [String] {
+        guard let dims = snapshot.dimensions, !dims.isEmpty else { return [] }
+        if let order = snapshot.dimensionOrder, !order.isEmpty {
+            return order.filter { dims[$0] != nil }
+        }
+        return dims.keys.sorted()
+    }
+
     var body: some View {
         VStack(alignment: .leading, spacing: 10) {
             HStack(spacing: 6) {
@@ -258,9 +305,24 @@ struct PluginGroupView: View {
                 stateView
             }
 
+            // Period segmented picker（仅当 plugin 输出了 dimensions 时显示）
+            if !orderedDimensionKeys.isEmpty {
+                Picker("", selection: Binding(
+                    get: { activeDimensionKey ?? "" },
+                    set: { selectedDimensionKey = $0 }
+                )) {
+                    ForEach(orderedDimensionKeys, id: \.self) { key in
+                        Text(snapshot.dimensions?[key]?.label ?? key).tag(key)
+                    }
+                }
+                .pickerStyle(.segmented)
+                .controlSize(.small)
+                .labelsHidden()
+            }
+
             Divider()
 
-            if snapshot.items.isEmpty {
+            if activeItems.isEmpty {
                 Text(emptyText)
                     .font(.callout)
                     .foregroundStyle(.secondary)
@@ -268,13 +330,13 @@ struct PluginGroupView: View {
                     .padding(.vertical, 18)
             } else {
                 VStack(spacing: 8) {
-                    ForEach(snapshot.items) { item in
+                    ForEach(activeItems) { item in
                         UsageItemRow(item: item, language: language)
                     }
                 }
             }
 
-            if let chart = snapshot.chart {
+            if let chart = activeChart {
                 Divider()
                 Button {
                     isChartExpanded.toggle()
@@ -358,24 +420,28 @@ struct UsageItemRow: View {
     var language: AppLanguage
 
     var body: some View {
-        HStack(spacing: 6) {
+        VStack(alignment: .leading, spacing: 4) {
+            // name 单独占一行（panel 全宽），让中文/长 hero 不被挤截
             Text(item.name)
+                .font(.callout)
                 .lineLimit(1)
                 .truncationMode(.tail)
-                .frame(width: 120, alignment: .leading)
+                .frame(maxWidth: .infinity, alignment: .leading)
 
-            UsageProgressBar(value: item.progress, label: item.displayValue(), color: item.color)
-                .frame(height: 18)
-                .layoutPriority(1)
+            // progress + trailing 共享下一行
+            HStack(spacing: 6) {
+                UsageProgressBar(value: item.progress, label: item.displayValue(), color: item.color)
+                    .frame(height: 18)
+                    .layoutPriority(1)
 
-            Text(item.resetText(language: language))
-                .font(.caption)
-                .lineLimit(1)
-                .minimumScaleFactor(0.8)
-                .foregroundStyle(.secondary)
-                .frame(width: 80, alignment: .trailing)
+                Text(item.trailingText ?? item.resetText(language: language))
+                    .font(.caption)
+                    .lineLimit(1)
+                    .minimumScaleFactor(0.85)
+                    .foregroundStyle(.secondary)
+                    .frame(width: 80, alignment: .trailing)
+            }
         }
-        .font(.callout)
     }
 }
 
diff --git a/Sources/UsageBoardCore/Models.swift b/Sources/UsageBoardCore/Models.swift
index 71e738c..7f70b24 100644
--- a/Sources/UsageBoardCore/Models.swift
+++ b/Sources/UsageBoardCore/Models.swift
@@ -343,12 +343,37 @@ public struct PluginOutput: Decodable, Equatable, Sendable {
     public var items: [UsageItem]
     public var badge: String?
     public var chart: PluginChart?
-
-    public init(updatedAt: Date, items: [UsageItem], badge: String? = nil, chart: PluginChart? = nil) {
+    public var dimensions: [String: PluginDimension]?
+    public var defaultDimension: String?
+    public var dimensionOrder: [String]?
+
+    public init(updatedAt: Date, items: [UsageItem], badge: String? = nil,
+                chart: PluginChart? = nil,
+                dimensions: [String: PluginDimension]? = nil,
+                defaultDimension: String? = nil,
+                dimensionOrder: [String]? = nil) {
         self.updatedAt = updatedAt
         self.items = items
         self.badge = badge
         self.chart = chart
+        self.dimensions = dimensions
+        self.defaultDimension = defaultDimension
+        self.dimensionOrder = dimensionOrder
+    }
+}
+
+/// Plugin 多维度输出（7d / 30d / 90d / all 的 items + chart 包）。
+public struct PluginDimension: Codable, Equatable, Sendable {
+    public var label: String
+    public var bucketUnit: String
+    public var items: [UsageItem]
+    public var chart: PluginChart?
+
+    public init(label: String, bucketUnit: String, items: [UsageItem], chart: PluginChart? = nil) {
+        self.label = label
+        self.bucketUnit = bucketUnit
+        self.items = items
+        self.chart = chart
     }
 }
 
@@ -411,6 +436,7 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
     public var resetAt: Date?
     public var status: UsageStatus
     public var color: String?
+    public var trailingText: String?
 
     public init(
         id: String,
@@ -420,7 +446,8 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
         displayStyle: UsageDisplayStyle,
         resetAt: Date? = nil,
         status: UsageStatus = .unknown,
-        color: String? = nil
+        color: String? = nil,
+        trailingText: String? = nil
     ) {
         self.id = id
         self.name = name
@@ -430,6 +457,7 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
         self.resetAt = resetAt
         self.status = status
         self.color = color
+        self.trailingText = trailingText
     }
 
     public var progress: Double {
@@ -527,6 +555,9 @@ public struct PluginSnapshot: Equatable, Identifiable, Sendable {
     public var badge: String?
     public var iconURL: String?
     public var chart: PluginChart?
+    public var dimensions: [String: PluginDimension]?
+    public var defaultDimension: String?
+    public var dimensionOrder: [String]?
 
     public init(
         id: UUID,
@@ -537,7 +568,10 @@ public struct PluginSnapshot: Equatable, Identifiable, Sendable {
         updatedAt: Date? = nil,
         badge: String? = nil,
         iconURL: String? = nil,
-        chart: PluginChart? = nil
+        chart: PluginChart? = nil,
+        dimensions: [String: PluginDimension]? = nil,
+        defaultDimension: String? = nil,
+        dimensionOrder: [String]? = nil
     ) {
         self.id = id
         self.pluginName = pluginName
@@ -548,6 +582,9 @@ public struct PluginSnapshot: Equatable, Identifiable, Sendable {
         self.badge = badge
         self.iconURL = iconURL
         self.chart = chart
+        self.dimensions = dimensions
+        self.defaultDimension = defaultDimension
+        self.dimensionOrder = dimensionOrder
     }
 }
 
@@ -556,11 +593,21 @@ public struct PluginCachedState: Codable, Equatable, Sendable {
     public var items: [UsageItem]
     public var badge: String?
     public var chart: PluginChart?
-
-    public init(updatedAt: Date, items: [UsageItem], badge: String? = nil, chart: PluginChart? = nil) {
+    public var dimensions: [String: PluginDimension]?
+    public var defaultDimension: String?
+    public var dimensionOrder: [String]?
+
+    public init(updatedAt: Date, items: [UsageItem], badge: String? = nil,
+                chart: PluginChart? = nil,
+                dimensions: [String: PluginDimension]? = nil,
+                defaultDimension: String? = nil,
+                dimensionOrder: [String]? = nil) {
         self.updatedAt = updatedAt
         self.items = items
         self.badge = badge
         self.chart = chart
+        self.dimensions = dimensions
+        self.defaultDimension = defaultDimension
+        self.dimensionOrder = dimensionOrder
     }
 }
diff --git a/Sources/UsageBoardCore/PluginExecutor.swift b/Sources/UsageBoardCore/PluginExecutor.swift
index cabe8d4..00c184d 100644
--- a/Sources/UsageBoardCore/PluginExecutor.swift
+++ b/Sources/UsageBoardCore/PluginExecutor.swift
@@ -3,7 +3,7 @@
 public struct PluginExecutor: Sendable {
     public var timeoutSeconds: TimeInterval
 
-    public init(timeoutSeconds: TimeInterval = 15) {
+    public init(timeoutSeconds: TimeInterval = 30) {
         self.timeoutSeconds = timeoutSeconds
     }
 
@@ -61,7 +61,10 @@ public struct PluginExecutor: Sendable {
                 updatedAt: pluginOutput.updatedAt,
                 badge: pluginOutput.badge,
                 iconURL: configuration.metadata?.icon,
-                chart: pluginOutput.chart
+                chart: pluginOutput.chart,
+                dimensions: pluginOutput.dimensions,
+                defaultDimension: pluginOutput.defaultDimension,
+                dimensionOrder: pluginOutput.dimensionOrder
             )
         } catch {
             return failed(configuration: configuration, displayName: displayName, message: "\(text(.jsonParseFailed, language: language))\(error.localizedDescription)")
