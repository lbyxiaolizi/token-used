diff --git a/Package.swift b/Package.swift
index 36fecf5..e4d6e73 100644
--- a/Package.swift
+++ b/Package.swift
@@ -1,4 +1,4 @@
-// swift-tools-version: 6.3
+// swift-tools-version: 6.2
 // The swift-tools-version declares the minimum version of Swift required to build this package.
 
 import PackageDescription
diff --git a/Sources/UsageBoardApp/DashboardView.swift b/Sources/UsageBoardApp/DashboardView.swift
index 07757ee..3fc228c 100644
--- a/Sources/UsageBoardApp/DashboardView.swift
+++ b/Sources/UsageBoardApp/DashboardView.swift
@@ -5,6 +5,7 @@ import UsageBoardCore
 struct DashboardView: View {
     @ObservedObject var store: UsageBoardStore
     var mode: DisplayMode
+    @AppStorage("usageboard.period.global") private var selectedDimensionKey = "30d"
 
     private var maxHeight: CGFloat {
         (NSScreen.main?.visibleFrame.height ?? 800) * 2 / 3
@@ -14,10 +15,35 @@ struct DashboardView: View {
         store.configuration.plugins.filter(\.enabled)
     }
 
+    /// 已加载完成且所有维度都为空的 plugin 自动隐藏（数据为 0 时不占位）。
+    /// idle / loading / failed 状态保留显示，避免首次加载误判。
+    private var visiblePlugins: [PluginConfiguration] {
+        enabledPlugins.filter { plugin in
+            let snapshot = store.snapshot(for: plugin)
+            if snapshot.state == .ready, snapshot.hasNoUsageItems { return false }
+            return true
+        }
+    }
+
     private var strings: AppLocalization {
         AppLocalization(language: store.activeLanguage)
     }
 
+    private var availableDimensionKeys: [String] {
+        var seen = Set<String>()
+        var keys: [String] = []
+        for plugin in visiblePlugins {
+            let snapshot = store.snapshot(for: plugin)
+            guard let dimensions = snapshot.dimensions, !dimensions.isEmpty else { continue }
+            let ordered = snapshot.dimensionOrder?.filter { dimensions[$0] != nil } ?? dimensions.keys.sorted()
+            for key in ordered where !seen.contains(key) {
+                seen.insert(key)
+                keys.append(key)
+            }
+        }
+        return keys
+    }
+
     var body: some View {
         Group {
             if enabledPlugins.isEmpty {
@@ -27,10 +53,12 @@ struct DashboardView: View {
                 case .grouped:
                     MeasuredScrollView(maxHeight: maxHeight) {
                         VStack(spacing: 8) {
-                            ForEach(enabledPlugins) { plugin in
+                            globalDimensionPicker
+                            ForEach(visiblePlugins) { plugin in
                                 PluginGroupView(
                                     snapshot: store.snapshot(for: plugin),
                                     language: store.activeLanguage,
+                                    selectedDimensionKey: selectedDimensionKey,
                                     nextRefreshAt: store.nextRefreshAt[plugin.id]
                                 ) {
                                     store.refresh(pluginID: plugin.id, force: true)
@@ -45,7 +73,7 @@ struct DashboardView: View {
                         ScrollViewReader { proxy in
                             ScrollView(.horizontal, showsIndicators: false) {
                                 HStack(spacing: 6) {
-                                    ForEach(enabledPlugins) { plugin in
+                                    ForEach(visiblePlugins) { plugin in
                                         Button {
                                             store.selectedTabID = plugin.id
                                         } label: {
@@ -72,18 +100,22 @@ struct DashboardView: View {
                             .onChange(of: store.selectedTabID) { _ in
                                 scrollToSelectedTab(with: proxy)
                             }
-                            .onChange(of: enabledPlugins.map(\.id)) { _ in
+                            .onChange(of: visiblePlugins.map(\.id)) { _ in
                                 scrollToSelectedTab(with: proxy)
                             }
                         }
                         Divider()
                         if let plugin = selectedPlugin {
-                            PluginGroupView(
-                                snapshot: store.snapshot(for: plugin),
-                                language: store.activeLanguage,
-                                nextRefreshAt: store.nextRefreshAt[plugin.id]
-                            ) {
-                                store.refresh(pluginID: plugin.id, force: true)
+                            VStack(spacing: 8) {
+                                globalDimensionPicker
+                                PluginGroupView(
+                                    snapshot: store.snapshot(for: plugin),
+                                    language: store.activeLanguage,
+                                    selectedDimensionKey: selectedDimensionKey,
+                                    nextRefreshAt: store.nextRefreshAt[plugin.id]
+                                ) {
+                                    store.refresh(pluginID: plugin.id, force: true)
+                                }
                             }
                             .padding(10)
                         }
@@ -93,8 +125,9 @@ struct DashboardView: View {
         }
         .onAppear {
             ensureSelectedTab()
+            store.refreshAll()
         }
-        .onChange(of: enabledPlugins.map(\.id)) { _ in
+        .onChange(of: visiblePlugins.map(\.id)) { _ in
             ensureSelectedTab()
         }
         .toolbar {
@@ -109,22 +142,22 @@ struct DashboardView: View {
 
     private var selectedPlugin: PluginConfiguration? {
         if let selectedTabID = store.selectedTabID,
-           let plugin = enabledPlugins.first(where: { $0.id == selectedTabID }) {
+           let plugin = visiblePlugins.first(where: { $0.id == selectedTabID }) {
             return plugin
         }
-        return enabledPlugins.first
+        return visiblePlugins.first
     }
 
     private func ensureSelectedTab() {
-        guard !enabledPlugins.isEmpty else {
+        guard !visiblePlugins.isEmpty else {
             store.selectedTabID = nil
             return
         }
         if let selectedTabID = store.selectedTabID,
-           enabledPlugins.contains(where: { $0.id == selectedTabID }) {
+           visiblePlugins.contains(where: { $0.id == selectedTabID }) {
             return
         }
-        store.selectedTabID = enabledPlugins.first?.id
+        store.selectedTabID = visiblePlugins.first?.id
     }
 
     private func scrollToSelectedTab(with proxy: ScrollViewProxy) {
@@ -133,6 +166,31 @@ struct DashboardView: View {
             proxy.scrollTo(selectedTabID, anchor: .center)
         }
     }
+
+    @ViewBuilder
+    private var globalDimensionPicker: some View {
+        let keys = availableDimensionKeys
+        if !keys.isEmpty {
+            Picker("", selection: $selectedDimensionKey) {
+                ForEach(keys, id: \.self) { key in
+                    Text(dimensionLabel(for: key)).tag(key)
+                }
+            }
+            .pickerStyle(.segmented)
+            .controlSize(.small)
+            .labelsHidden()
+        }
+    }
+
+    private func dimensionLabel(for key: String) -> String {
+        for plugin in visiblePlugins {
+            let snapshot = store.snapshot(for: plugin)
+            if let label = snapshot.dimensions?[key]?.label {
+                return label
+            }
+        }
+        return key
+    }
 }
 
 struct EmptyPluginsView: View {
@@ -224,13 +282,54 @@ private struct ContentHeightKey: PreferenceKey {
 struct PluginGroupView: View {
     var snapshot: PluginSnapshot
     var language: AppLanguage
+    var selectedDimensionKey: String?
     var nextRefreshAt: Date?
     var onRefresh: (() -> Void)?
     @State private var isChartExpanded = false
+    @State private var didCopySubtitle = false
     private var strings: AppLocalization {
         AppLocalization(language: language)
     }
 
+    private var activeDimensionKey: String? {
+        guard let dimensions = snapshot.dimensions, !dimensions.isEmpty else { return nil }
+        if let selectedDimensionKey, dimensions[selectedDimensionKey] != nil {
+            return selectedDimensionKey
+        }
+        if let defaultDimension = snapshot.defaultDimension, dimensions[defaultDimension] != nil {
+            return defaultDimension
+        }
+        return snapshot.dimensionOrder?.first { dimensions[$0] != nil } ?? dimensions.keys.sorted().first
+    }
+
+    private var activeItems: [UsageItem] {
+        if let key = activeDimensionKey, let dimension = snapshot.dimensions?[key] {
+            return dimension.items
+        }
+        return snapshot.items
+    }
+
+    private var activeChart: PluginChart? {
+        if let key = activeDimensionKey, let dimension = snapshot.dimensions?[key] {
+            return dimension.chart
+        }
+        return snapshot.chart
+    }
+
+    private var activeBadge: String? {
+        if let key = activeDimensionKey, let badge = snapshot.dimensions?[key]?.badge {
+            return badge
+        }
+        return activeItems.first?.trailingText ?? snapshot.badge
+    }
+
+    private var activeIconURL: String? {
+        if let key = activeDimensionKey, let icon = snapshot.dimensions?[key]?.iconURL {
+            return icon
+        }
+        return snapshot.iconURL
+    }
+
     var body: some View {
         VStack(alignment: .leading, spacing: 0) {
             VStack(alignment: .leading, spacing: 10) {
@@ -251,7 +350,7 @@ struct PluginGroupView: View {
                             .textSelection(.enabled)
                     }
                     .padding(.vertical, 8)
-                } else if snapshot.items.isEmpty {
+                } else if activeItems.isEmpty {
                     Text(strings.text(.noUsageData))
                         .font(.callout)
                         .foregroundStyle(.secondary)
@@ -259,7 +358,7 @@ struct PluginGroupView: View {
                         .padding(.vertical, 18)
                 } else {
                     VStack(spacing: 8) {
-                        ForEach(snapshot.items) { item in
+                        ForEach(activeItems) { item in
                             UsageItemRow(item: item, language: language)
                         }
                     }
@@ -267,9 +366,9 @@ struct PluginGroupView: View {
             }
             .padding(.horizontal, 12)
             .padding(.top, 10)
-            .padding(.bottom, snapshot.chart == nil ? 10 : 0)
+            .padding(.bottom, activeChart == nil ? 10 : 0)
 
-            if let chart = snapshot.chart {
+            if let chart = activeChart {
                 VStack(spacing: 0) {
                     if isChartExpanded {
                         TokenUsageChartView(chart: chart, language: language)
@@ -306,12 +405,36 @@ struct PluginGroupView: View {
 
     private var header: some View {
         HStack(spacing: 8) {
-            BrandTile(iconURL: snapshot.iconURL, fallbackName: snapshot.displayName, size: 22)
-            Text(snapshot.displayName)
-                .font(UB.Font.cardTitle)
-                .lineLimit(1)
-            if let badge = snapshot.badge {
-                PlanTag(text: badge)
+            BrandTile(iconURL: activeIconURL, fallbackName: snapshot.displayName, size: 22)
+            if let subtitle = snapshot.subtitle, !subtitle.isEmpty {
+                VStack(alignment: .leading, spacing: 1) {
+                    Text(snapshot.displayName)
+                        .font(UB.Font.cardTitle)
+                        .lineLimit(1)
+                    Text(didCopySubtitle ? (language == .en ? "Copied" : "已复制") : subtitle)
+                        .font(.system(size: 10))
+                        .foregroundStyle(didCopySubtitle ? Color.accentColor : Color.secondary)
+                        .lineLimit(1)
+                        .truncationMode(.middle)
+                        .contentShape(Rectangle())
+                        .onTapGesture {
+                            NSPasteboard.general.clearContents()
+                            NSPasteboard.general.setString(subtitle, forType: .string)
+                            didCopySubtitle = true
+                            Task {
+                                try? await Task.sleep(nanoseconds: 1_200_000_000)
+                                didCopySubtitle = false
+                            }
+                        }
+                        .help(language == .en ? "\(subtitle) (click to copy)" : "\(subtitle)（点击复制）")
+                }
+            } else {
+                Text(snapshot.displayName)
+                    .font(UB.Font.cardTitle)
+                    .lineLimit(1)
+            }
+            if let badge = activeBadge {
+                PlanTag(text: badge, size: 14)
             }
             if case .failed = snapshot.state {
                 Text(language == .en ? "Error" : "错误")
@@ -364,13 +487,13 @@ struct UsageItemRow: View {
                 .foregroundStyle(.secondary)
                 .lineLimit(1)
                 .truncationMode(.tail)
-                .frame(width: 92, alignment: .leading)
+                .frame(width: 122, alignment: .leading)
 
             UsageProgressBar(value: item.progress, label: item.displayValue(), color: item.color)
                 .frame(height: 18)
                 .layoutPriority(1)
 
-            Text(item.resetText(language: language))
+            Text(item.trailingText ?? item.resetText(language: language))
                 .font(.system(size: 11))
                 .monospacedDigit()
                 .lineLimit(1)
@@ -413,7 +536,8 @@ struct TokenUsageChartView: View {
     private var visibleSeries: [TokenChartSeries] {
         let filtered = series.filter { $0.values.contains(where: { $0 > 0 }) }
         guard let selectedSeries else { return filtered }
-        return filtered.filter { $0.name == selectedSeries }
+        let selected = filtered.filter { $0.name == selectedSeries }
+        return selected.isEmpty ? filtered : selected
     }
 
     private var modelSummaries: [TokenModelSummary] {
@@ -481,20 +605,28 @@ struct TokenUsageChartView: View {
                     }
                 }
 
-                GeometryReader { viewport in
-                    ScrollView(.horizontal, showsIndicators: false) {
-                        let resolvedWidth = resolvedChartWidth(for: viewport.size.width)
-                        TokenLineChartPlot(
-                            buckets: chart.buckets,
-                            series: visibleSeries,
-                            maxValue: max(visibleSeries.flatMap(\.values).max() ?? 0, 1),
-                            visibleWidth: viewport.size.width
-                        )
-                        .frame(width: resolvedWidth, height: 170)
+                if chart.prefersSingleBucketSummary, let bucket = chart.buckets.first {
+                    TokenSingleBucketChartView(
+                        bucket: bucket,
+                        series: visibleSeries,
+                        maxValue: max(visibleSeries.flatMap(\.values).max() ?? 0, 1)
+                    )
+                } else {
+                    GeometryReader { viewport in
+                        ScrollView(.horizontal, showsIndicators: false) {
+                            let resolvedWidth = resolvedChartWidth(for: viewport.size.width)
+                            TokenLineChartPlot(
+                                buckets: chart.buckets,
+                                series: visibleSeries,
+                                maxValue: max(visibleSeries.flatMap(\.values).max() ?? 0, 1),
+                                visibleWidth: viewport.size.width
+                            )
+                            .frame(width: resolvedWidth, height: 170)
+                        }
+                        .coordinateSpace(name: "TokenChartScroll")
                     }
-                    .coordinateSpace(name: "TokenChartScroll")
+                    .frame(height: 170)
                 }
-                .frame(height: 170)
             } else {
                 Text(chart.message ?? strings.text(.noStatsData))
                     .font(.caption)
@@ -537,6 +669,94 @@ struct TokenUsageChartView: View {
     }
 }
 
+struct TokenSingleBucketChartView: View {
+    var bucket: PluginChartBucket
+    var series: [TokenChartSeries]
+    var maxValue: Double
+
+    private var rows: [TokenChartSeries] {
+        let nonzero = series.filter { ($0.values.first ?? 0) > 0 }
+        if nonzero.count > 1 {
+            return Array(nonzero.dropFirst())
+        }
+        return nonzero
+    }
+
+    var body: some View {
+        VStack(alignment: .leading, spacing: 10) {
+            HStack(alignment: .firstTextBaseline) {
+                Text(bucket.label)
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+                    .lineLimit(1)
+                Spacer(minLength: 8)
+                Text(formattedTokenNumber(bucket.total).compact)
+                    .font(.caption)
+                    .foregroundStyle(.primary)
+                    .monospacedDigit()
+            }
+
+            VStack(spacing: 8) {
+                ForEach(rows) { item in
+                    TokenSingleBucketBarRow(
+                        title: item.name,
+                        value: item.values.first ?? 0,
+                        maxValue: maxValue,
+                        color: item.color
+                    )
+                }
+            }
+        }
+        .padding(10)
+        .background(
+            RoundedRectangle(cornerRadius: 6, style: .continuous)
+                .fill(Color(nsColor: .textBackgroundColor).opacity(0.55))
+        )
+    }
+}
+
+struct TokenSingleBucketBarRow: View {
+    var title: String
+    var value: Double
+    var maxValue: Double
+    var color: Color
+
+    private var widthRatio: CGFloat {
+        guard maxValue > 0 else { return 0 }
+        return CGFloat(max(0, min(value / maxValue, 1)))
+    }
+
+    var body: some View {
+        VStack(alignment: .leading, spacing: 5) {
+            HStack(spacing: 6) {
+                Circle()
+                    .fill(color)
+                    .frame(width: 7, height: 7)
+                Text(title)
+                    .font(.caption)
+                    .foregroundStyle(.secondary)
+                    .lineLimit(1)
+                Spacer(minLength: 8)
+                Text(formattedTokenNumber(value).compact)
+                    .font(.caption)
+                    .foregroundStyle(.primary)
+                    .monospacedDigit()
+            }
+
+            GeometryReader { proxy in
+                ZStack(alignment: .leading) {
+                    Capsule()
+                        .fill(Color(nsColor: .separatorColor).opacity(0.45))
+                    Capsule()
+                        .fill(color.opacity(0.72))
+                        .frame(width: max(widthRatio * proxy.size.width, value > 0 ? 4 : 0))
+                }
+            }
+            .frame(height: 6)
+        }
+    }
+}
+
 struct TokenModelSummary: Identifiable {
     var name: String
     var total: Double
diff --git a/Sources/UsageBoardApp/DesignSystem/BrandTile.swift b/Sources/UsageBoardApp/DesignSystem/BrandTile.swift
index 072840d..9390019 100644
--- a/Sources/UsageBoardApp/DesignSystem/BrandTile.swift
+++ b/Sources/UsageBoardApp/DesignSystem/BrandTile.swift
@@ -26,11 +26,13 @@ struct BrandTile: View {
         let radius = size * 0.27
         ZStack {
             if let image = loadedImage {
+                RoundedRectangle(cornerRadius: radius, style: .continuous)
+                    .fill(Color.white)
                 Image(nsImage: image)
                     .resizable()
                     .interpolation(.high)
                     .aspectRatio(contentMode: .fit)
-                    .padding(size * 0.08)
+                    .padding(size * 0.12)
             } else {
                 RoundedRectangle(cornerRadius: radius, style: .continuous)
                     .fill(fallbackGradient)
@@ -43,7 +45,7 @@ struct BrandTile: View {
         .clipShape(RoundedRectangle(cornerRadius: radius, style: .continuous))
         .overlay(
             RoundedRectangle(cornerRadius: radius, style: .continuous)
-                .stroke(Color.black.opacity(0.06), lineWidth: 0.5)
+                .stroke(Color.black.opacity(0.10), lineWidth: 0.5)
         )
         .task(id: iconURL) {
             guard let urlString = iconURL, let url = URL(string: urlString) else {
diff --git a/Sources/UsageBoardApp/DesignSystem/PlanTag.swift b/Sources/UsageBoardApp/DesignSystem/PlanTag.swift
index b923f4a..868ddd4 100644
--- a/Sources/UsageBoardApp/DesignSystem/PlanTag.swift
+++ b/Sources/UsageBoardApp/DesignSystem/PlanTag.swift
@@ -2,16 +2,18 @@ import SwiftUI
 
 struct PlanTag: View {
     var text: String
+    var size: CGFloat = 9.5
 
     var body: some View {
         Text(text.uppercased())
-            .font(.system(size: 9.5, weight: .bold))
+            .font(.system(size: size, weight: .bold))
             .tracking(0.5)
+            .monospacedDigit()
             .foregroundStyle(foreground)
-            .padding(.horizontal, 5)
-            .padding(.vertical, 1.5)
+            .padding(.horizontal, size * 0.55)
+            .padding(.vertical, size * 0.18)
             .background(
-                RoundedRectangle(cornerRadius: 4, style: .continuous)
+                RoundedRectangle(cornerRadius: max(4, size * 0.35), style: .continuous)
                     .fill(background)
             )
     }
@@ -29,7 +31,7 @@ struct PlanTag: View {
         case "MAX", "MAX 5X", "MAX 20X":
             return (Color.orange.opacity(0.18), Color.orange)
         default:
-            return (Color.gray.opacity(0.16), Color.secondary)
+            return (Color.primary.opacity(0.10), Color.primary.opacity(0.85))
         }
     }
 
diff --git a/Sources/UsageBoardApp/DesignSystem/UBDesignTokens.swift b/Sources/UsageBoardApp/DesignSystem/UBDesignTokens.swift
index a787db7..0861b5c 100644
--- a/Sources/UsageBoardApp/DesignSystem/UBDesignTokens.swift
+++ b/Sources/UsageBoardApp/DesignSystem/UBDesignTokens.swift
@@ -34,7 +34,7 @@ enum UB {
 
     enum Canvas {
         static let popoverBackground = Color(nsColor: .windowBackgroundColor)
-        static let canvasBackground = Color(red: 0.961, green: 0.961, blue: 0.969)
+        static let canvasBackground = Color(nsColor: .windowBackgroundColor)
         static let cardBackground = Color(nsColor: .textBackgroundColor)
         static let separator = Color(nsColor: .separatorColor)
     }
diff --git a/Sources/UsageBoardApp/UsageBoardStore.swift b/Sources/UsageBoardApp/UsageBoardStore.swift
index ecfb7de..81e1281 100644
--- a/Sources/UsageBoardApp/UsageBoardStore.swift
+++ b/Sources/UsageBoardApp/UsageBoardStore.swift
@@ -254,7 +254,8 @@ final class UsageBoardStore: ObservableObject {
             items: snapshots[plugin.id]?.items ?? [],
             updatedAt: snapshots[plugin.id]?.updatedAt,
             badge: snapshots[plugin.id]?.badge,
-            chart: snapshots[plugin.id]?.chart
+            chart: snapshots[plugin.id]?.chart,
+            subtitle: snapshots[plugin.id]?.subtitle
         )
 
         let executor = executor
@@ -283,7 +284,12 @@ final class UsageBoardStore: ObservableObject {
                     updatedAt: updatedAt,
                     items: snapshot.items,
                     badge: snapshot.badge,
-                    chart: snapshot.chart
+                    iconURL: snapshot.iconURL,
+                    chart: snapshot.chart,
+                    dimensions: snapshot.dimensions,
+                    defaultDimension: snapshot.defaultDimension,
+                    dimensionOrder: snapshot.dimensionOrder,
+                    subtitle: snapshot.subtitle
                 )
                 let stateID = current.stateID
                 Task.detached(priority: .utility) {
@@ -386,7 +392,12 @@ final class UsageBoardStore: ObservableObject {
                 items: cached.items,
                 updatedAt: cached.updatedAt,
                 badge: cached.badge,
-                chart: cached.chart
+                iconURL: cached.iconURL,
+                chart: cached.chart,
+                dimensions: cached.dimensions,
+                defaultDimension: cached.defaultDimension,
+                dimensionOrder: cached.dimensionOrder,
+                subtitle: cached.subtitle
             )
         }
     }
@@ -538,7 +549,12 @@ final class UsageBoardStore: ObservableObject {
         items: [UsageItem] = [],
         updatedAt: Date? = nil,
         badge: String? = nil,
-        chart: PluginChart? = nil
+        iconURL: String? = nil,
+        chart: PluginChart? = nil,
+        dimensions: [String: PluginDimension]? = nil,
+        defaultDimension: String? = nil,
+        dimensionOrder: [String]? = nil,
+        subtitle: String? = nil
     ) -> PluginSnapshot {
         PluginSnapshot(
             id: plugin.id,
@@ -548,8 +564,12 @@ final class UsageBoardStore: ObservableObject {
             items: items,
             updatedAt: updatedAt,
             badge: badge,
-            iconURL: plugin.metadata?.icon,
-            chart: chart
+            iconURL: iconURL ?? plugin.metadata?.icon,
+            chart: chart,
+            dimensions: dimensions,
+            defaultDimension: defaultDimension,
+            dimensionOrder: dimensionOrder,
+            subtitle: subtitle
         )
     }
 
diff --git a/Sources/UsageBoardCore/Models.swift b/Sources/UsageBoardCore/Models.swift
index 9a141f8..30588fd 100644
--- a/Sources/UsageBoardCore/Models.swift
+++ b/Sources/UsageBoardCore/Models.swift
@@ -344,13 +344,51 @@ public struct PluginOutput: Decodable, Equatable, Sendable {
     public var updatedAt: Date
     public var items: [UsageItem]
     public var badge: String?
+    public var iconURL: String?
     public var chart: PluginChart?
+    public var dimensions: [String: PluginDimension]?
+    public var defaultDimension: String?
+    public var dimensionOrder: [String]?
+    public var subtitle: String?
 
-    public init(updatedAt: Date, items: [UsageItem], badge: String? = nil, chart: PluginChart? = nil) {
+    public init(
+        updatedAt: Date,
+        items: [UsageItem],
+        badge: String? = nil,
+        iconURL: String? = nil,
+        chart: PluginChart? = nil,
+        dimensions: [String: PluginDimension]? = nil,
+        defaultDimension: String? = nil,
+        dimensionOrder: [String]? = nil,
+        subtitle: String? = nil
+    ) {
         self.updatedAt = updatedAt
         self.items = items
         self.badge = badge
+        self.iconURL = iconURL
         self.chart = chart
+        self.dimensions = dimensions
+        self.defaultDimension = defaultDimension
+        self.dimensionOrder = dimensionOrder
+        self.subtitle = subtitle
+    }
+}
+
+public struct PluginDimension: Codable, Equatable, Sendable {
+    public var label: String
+    public var bucketUnit: String
+    public var items: [UsageItem]
+    public var chart: PluginChart?
+    public var badge: String?
+    public var iconURL: String?
+
+    public init(label: String, bucketUnit: String, items: [UsageItem], chart: PluginChart? = nil, badge: String? = nil, iconURL: String? = nil) {
+        self.label = label
+        self.bucketUnit = bucketUnit
+        self.items = items
+        self.chart = chart
+        self.badge = badge
+        self.iconURL = iconURL
     }
 }
 
@@ -374,6 +412,10 @@ public struct PluginChart: Codable, Equatable, Sendable {
         self.buckets = buckets
         self.message = message
     }
+
+    public var prefersSingleBucketSummary: Bool {
+        buckets.count == 1 && buckets[0].segments.contains { $0.tokens > 0 }
+    }
 }
 
 public struct PluginChartBucket: Codable, Equatable, Identifiable, Sendable {
@@ -413,6 +455,7 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
     public var resetAt: Date?
     public var status: UsageStatus
     public var color: String?
+    public var trailingText: String?
 
     public init(
         id: String,
@@ -422,7 +465,8 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
         displayStyle: UsageDisplayStyle,
         resetAt: Date? = nil,
         status: UsageStatus = .unknown,
-        color: String? = nil
+        color: String? = nil,
+        trailingText: String? = nil
     ) {
         self.id = id
         self.name = name
@@ -432,6 +476,7 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
         self.resetAt = resetAt
         self.status = status
         self.color = color
+        self.trailingText = trailingText
     }
 
     public var progress: Double {
@@ -442,7 +487,16 @@ public struct UsageItem: Codable, Equatable, Identifiable, Sendable {
     public func displayValue() -> String {
         switch displayStyle {
         case .percent:
-            return "\(Int((progress * 100).rounded()))%"
+            let pct = progress * 100
+            if pct >= 10 || pct == 0 {
+                // 未到 100% 时不因四舍五入显示成 100%（99.8% → 99%）
+                let rounded = pct < 100 ? min(Int(pct.rounded()), 99) : 100
+                return "\(rounded)%"
+            }
+            if pct >= 1 {
+                return String(format: "%.1f%%", pct)
+            }
+            return String(format: "%.2f%%", pct)
         case .ratio:
             return "\(UsageItem.formatNumber(used)) / \(UsageItem.formatNumber(limit))"
         }
@@ -525,6 +579,10 @@ public struct PluginSnapshot: Equatable, Identifiable, Sendable {
     public var badge: String?
     public var iconURL: String?
     public var chart: PluginChart?
+    public var dimensions: [String: PluginDimension]?
+    public var defaultDimension: String?
+    public var dimensionOrder: [String]?
+    public var subtitle: String?
 
     public init(
         id: UUID,
@@ -535,7 +593,11 @@ public struct PluginSnapshot: Equatable, Identifiable, Sendable {
         updatedAt: Date? = nil,
         badge: String? = nil,
         iconURL: String? = nil,
-        chart: PluginChart? = nil
+        chart: PluginChart? = nil,
+        dimensions: [String: PluginDimension]? = nil,
+        defaultDimension: String? = nil,
+        dimensionOrder: [String]? = nil,
+        subtitle: String? = nil
     ) {
         self.id = id
         self.pluginName = pluginName
@@ -546,6 +608,20 @@ public struct PluginSnapshot: Equatable, Identifiable, Sendable {
         self.badge = badge
         self.iconURL = iconURL
         self.chart = chart
+        self.dimensions = dimensions
+        self.defaultDimension = defaultDimension
+        self.dimensionOrder = dimensionOrder
+        self.subtitle = subtitle
+    }
+
+    public var hasNoUsageItems: Bool {
+        if let dimensions, !dimensions.isEmpty {
+            if let key = defaultDimension, let dim = dimensions[key] {
+                return dim.items.isEmpty
+            }
+            return dimensions.values.allSatisfy { $0.items.isEmpty }
+        }
+        return items.isEmpty
     }
 }
 
@@ -553,12 +629,32 @@ public struct PluginCachedState: Codable, Equatable, Sendable {
     public var updatedAt: Date
     public var items: [UsageItem]
     public var badge: String?
+    public var iconURL: String?
     public var chart: PluginChart?
+    public var dimensions: [String: PluginDimension]?
+    public var defaultDimension: String?
+    public var dimensionOrder: [String]?
+    public var subtitle: String?
 
-    public init(updatedAt: Date, items: [UsageItem], badge: String? = nil, chart: PluginChart? = nil) {
+    public init(
+        updatedAt: Date,
+        items: [UsageItem],
+        badge: String? = nil,
+        iconURL: String? = nil,
+        chart: PluginChart? = nil,
+        dimensions: [String: PluginDimension]? = nil,
+        defaultDimension: String? = nil,
+        dimensionOrder: [String]? = nil,
+        subtitle: String? = nil
+    ) {
         self.updatedAt = updatedAt
         self.items = items
         self.badge = badge
+        self.iconURL = iconURL
         self.chart = chart
+        self.dimensions = dimensions
+        self.defaultDimension = defaultDimension
+        self.dimensionOrder = dimensionOrder
+        self.subtitle = subtitle
     }
 }
diff --git a/Sources/UsageBoardCore/PluginExecutor.swift b/Sources/UsageBoardCore/PluginExecutor.swift
index ead2d76..942f2c0 100644
--- a/Sources/UsageBoardCore/PluginExecutor.swift
+++ b/Sources/UsageBoardCore/PluginExecutor.swift
@@ -20,7 +20,7 @@ private final class DataBuffer: @unchecked Sendable {
 public struct PluginExecutor: Sendable {
     public var timeoutSeconds: TimeInterval
 
-    public init(timeoutSeconds: TimeInterval = 15) {
+    public init(timeoutSeconds: TimeInterval = 180) {
         self.timeoutSeconds = timeoutSeconds
     }
 
@@ -116,8 +116,12 @@ public struct PluginExecutor: Sendable {
                 items: pluginOutput.items,
                 updatedAt: pluginOutput.updatedAt,
                 badge: pluginOutput.badge,
-                iconURL: configuration.metadata?.icon,
-                chart: pluginOutput.chart
+                iconURL: pluginOutput.iconURL ?? configuration.metadata?.icon,
+                chart: pluginOutput.chart,
+                dimensions: pluginOutput.dimensions,
+                defaultDimension: pluginOutput.defaultDimension,
+                dimensionOrder: pluginOutput.dimensionOrder,
+                subtitle: pluginOutput.subtitle
             )
         } catch {
             if let errorOutput = try? JSONDecoder().decode(PluginOutputError.self, from: outputData),
diff --git a/Tests/UsageBoardTests/UsageBoardTests.swift b/Tests/UsageBoardTests/UsageBoardTests.swift
index a0955fd..c6706fc 100644
--- a/Tests/UsageBoardTests/UsageBoardTests.swift
+++ b/Tests/UsageBoardTests/UsageBoardTests.swift
@@ -134,6 +134,41 @@ final class UsageBoardTests: XCTestCase {
         XCTAssertEqual(decoded.chart?.buckets.first?.label, "05-01")
     }
 
+    func testPluginChartUsesSingleBucketSummaryOnlyForSingleDataBucket() {
+        let singleBucket = PluginChart(
+            period: "today",
+            bucketUnit: "day",
+            buckets: [
+                PluginChartBucket(
+                    id: "2026-05-18",
+                    label: "今日",
+                    segments: [PluginChartSegment(model: "gpt-5.5", tokens: 1_200)]
+                )
+            ]
+        )
+        let emptySingleBucket = PluginChart(
+            period: "today",
+            bucketUnit: "day",
+            buckets: [PluginChartBucket(id: "2026-05-18", label: "今日", segments: [])]
+        )
+        let multiBucket = PluginChart(
+            period: "7d",
+            bucketUnit: "day",
+            buckets: [
+                PluginChartBucket(id: "2026-05-17", label: "05-17", segments: []),
+                PluginChartBucket(
+                    id: "2026-05-18",
+                    label: "05-18",
+                    segments: [PluginChartSegment(model: "gpt-5.5", tokens: 1_200)]
+                )
+            ]
+        )
+
+        XCTAssertTrue(singleBucket.prefersSingleBucketSummary)
+        XCTAssertFalse(emptySingleBucket.prefersSingleBucketSummary)
+        XCTAssertFalse(multiBucket.prefersSingleBucketSummary)
+    }
+
     func testPluginExecutorPropagatesChart() throws {
         let script = """
         import json
